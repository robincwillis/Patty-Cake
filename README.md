# P A T T Y C A K E
## Electron React

````
# install dependencies
~ npm install

# start Electron
~ npm start

# watch for file changes
~ npm run watch

# reset
~ rm -rf node_modules && npm install
````